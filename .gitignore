/bin/
/dist/
coverage.out
coverage.html

# abel's own state, when abel is run against this repo.
.abel/

# Crashers under testdata/fuzz/ are committed on purpose: they are the
# regression corpus. Nothing here is ignored.
